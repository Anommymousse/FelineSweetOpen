using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Net.Configuration;
using TMPro;
using UnityEngine;

public class LogScreenMessage : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
    }

    public void Log(string displayText)
    {
        //debugscreenlog.SetText(displayText);
    }
    
    // Update is called once per frame
    void Update()
    {
        
    }
}
