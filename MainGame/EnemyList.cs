using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyList : MonoBehaviour
{
    /*
    List<GameObject> enemyList;
    // Start is called before the first frame update
    void Start()
    {
        enemyList = new List<GameObject>();
        var goStartList = GetComponentsInChildren<GameObject>();
        foreach (var go in goStartList)
        {
            enemyList.Add(go);
        }
    }

    public void AddToList(GameObject gameObjectToAdd)
    {
        enemyList.Add(gameObjectToAdd);
    }

    public void RemoveFromList(GameObject gameObjectToRemove)
    {
        enemyList.Remove(gameObjectToRemove);
    }
    
    

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
